# Getting Started

Install:

```sh
npm install hvn-anti-ai-ui @emotion/react @emotion/styled
```

Import:

```tsx
import { AntiHero, ButtonWarp } from 'hvn-anti-ai-ui';
```

Use the components where generic defaults would weaken the work: landing pages, operational dashboards, product onboarding, and modular marketing surfaces.
